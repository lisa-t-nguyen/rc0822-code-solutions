/* exported setValue */
// define function setValue with 3 parameters, object, key, and value
// set object to [key] with the assignment operator to value
function setValue(object, key, value) {
  object[key] = value;
}
